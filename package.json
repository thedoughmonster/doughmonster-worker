{
  "name": "doughmonster-worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "rm -rf dist && npm run build --silent && node --test tests/*.mjs",
    "docs": "node scripts/generate-openapi.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.42.2"
  }
}
