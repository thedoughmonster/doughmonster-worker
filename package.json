{
  "name": "doughmonster-worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "rm -rf dist && npm run build --silent && node --test tests/*.mjs"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.3",
    "typescript": "^5.6.3",
    "wrangler": "^4.42.2"
  }
}
